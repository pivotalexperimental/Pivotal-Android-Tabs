<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.pivotallabs"
      android:versionCode="1"
      android:versionName="1.0">

    <application android:label="tabs" android:icon="@drawable/icon">

        <activity
                android:name=".ExampleTabActivity"
                android:label="@string/app_name"
                android:theme="@android:style/Theme.NoTitleBar"
                >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".HelloActivity" android:label="@string/app_name"/>
        <activity android:name=".GoodbyeActivity" android:label="@string/app_name"/>
        <activity android:name=".OtherActivity" android:label="@string/app_name"/>

    </application>

</manifest>
